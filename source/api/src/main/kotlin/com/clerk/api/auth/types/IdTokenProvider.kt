package com.clerk.api.auth.types

/**
 * ID token provider enum.
 *
 * Represents identity providers that can issue ID tokens for authentication.
 */
enum class IdTokenProvider {
  /** Google ID token from Google Identity Services. */
  GOOGLE
}
