# Authentication entry-point migration

The generated authentication resources replace the old native `Auth` wrapper. This audit reviews all 23 declarations and complete fixture/mock bodies in `AuthHandleTest`, `AuthOtpTest`, `AuthStartHostedAuthTest` and `AuthTest`, alongside baseline `Auth.kt` and the current shared implementation. The four old files are retired. Their exact declaration names and baseline hashes remain in [legacy-tests.json](legacy-tests.json), at baseline `1ea9f97250e9e3b266b7fbcfe37d9373e4fc393f`.

The replacement is not an API-compatible wrapper. Calls return generated resources/unions or `Unit`, and failures throw structured `CoreException`. Completed authentication still requires `finalize()`. The old `AuthEvent.Error` singleton stream, local session-restoration helpers and hosted-portal protocol are not recreated.

## Google identity and identifier methods

`GoogleIdentityContractTest` executes nine scenarios through the public Kotlin API, packaged QuickJS, generated dispatch and the shared Google flow. It checks existing accounts, account creation with nested metadata, sign-in-only mode, rejected identity, rejected sign-up, empty-picker browser fallback, cancellation, blank tokens and reset while a late identity result is pending. Successful cases check no selected session before finalization and the selected session/user afterward. Failures preserve server status, code, long message and trace; cancellation and invalid identity results do not issue authentication HTTP requests.

Only `google_account_unavailable` falls back to browser OAuth. Cancellation, provider rejection and sign-up restriction stay terminal. The fixture supplies the identity token; these checks do not exercise a real Credential Manager picker or verify a Google account against a live backend.

`AuthEntryPointTest` checks separate create and code-preparation requests for email and phone, selected factor IDs, device locale, absence of email-link redirect/PKCE fields, nested sign-up metadata and unrelated callback rejection. These are future-style operations: the old `signInWithOtp` request combining identifier and strategy is intentionally replaced by explicit preparation.

## Assertion dispositions

| Legacy declaration | Current disposition and evidence |
| --- | --- |
| `handle completes native magic link callback without SSO fallback` | Packaged `EmailLinkCompletionTest` checks valid imported Android state, ticket redemption, generated result identity and no implicit selection; `PackagedCoreTest.previousNativeEmailLinkCallbackFormsCompleteWithoutActivation` checks the old callback forms. Generated `magic-link.test.mjs` checks route matching, shared concurrent redemption and restart continuity. |
| `handle completes SSO callback when URI is Clerk scheme and not magic link` | Intentional routing change: `handleAuthCallback` is the shared native email-link entry point, not a scheme-prefix SSO dispatcher. `AuthEntryPointTest.unrelatedAndOAuthCallbacksAreNotRedeemedAsEmailLinks` verifies both old and configured OAuth-only routes return null without HTTP. Browser OAuth callbacks finish inside the native browser capability and shared `sso` transport; generated `sso.test.mjs` checks nonce reconciliation and unrelated callback rejection. |
| `handle completes hosted auth callback before SSO fallback` | Removed with the unsupported hosted-portal protocol. No priority ordering between removed native services remains. Browser OAuth and email-link support do not establish hosted-portal support. |
| `handle returns false for non Clerk URIs` | The generated method returns null for an unrelated URI, with no requests or callback state. Tested by `AuthEntryPointTest` and shared magic-link route fixtures. |
| `handle returns false for null URI` | The generated Kotlin parameter is nonnullable `URI`; callers handle missing intent data before calling it. The old nullable Boolean helper is removed. |
| `signInWithOtp for email creates email code flow instead of native email link` | `emailCodeUsesExplicitPreparation` checks the email identifier and device locale on create, then a separate email-code preparation for the selected ID. No redirect URI, redirect URL, PKCE challenge or pending magic-link record is created. The old no-preparation assertion is intentionally retired. |
| `startHostedAuth activates the created session via setActive` | Removed with the unsupported hosted-portal protocol. Generated `signIn.finalize` / `signUp.finalize` are explicit; the Google cases check selection only after calling them. This is not hosted-protocol equivalence. |
| `startHostedAuth succeeds under forced organization selection using the applied session` | Removed with hosted auth. The baseline assertion depends on that protocol's early native session application; it is not a generated API requirement. Pending-task and explicit-organization behavior have separate [audits](session-task-test-audit.md). |
| `startHostedAuth propagates hosted auth failures without activation` | Removed with hosted auth. Google server, cancellation and restriction cases independently verify structured failure without session selection for supported operations; they do not validate the removed hosted failure type. |
| `signUpWithGoogleOneTap delegates to transferable Google One Tap flow` | The nine-case packaged Google suite invokes `authenticateWithSSO` with the Google preference and explicit transfer option. The new-account case returns the owner's sign-up resource with metadata; the existing-account case returns its sign-in resource. No mock of a Kotlin companion is needed. |
| `signUpWithOAuth delegates to sign-up redirect flow` | `PackagedCoreTest.generatedApiUsesPackagedCore` invokes generated `signUp.sso`, checks complete state without selection, and preserves the OIDC prompt. Shared `sso.test.mjs` executes sign-up preparation, browser transport and nonce reconciliation. The old service delegation itself is removed. |
| `signUp forwards unsafe metadata` | `AuthEntryPointTest.signupPreservesNestedMetadata` verifies the actual form request's email and nested JSON Boolean. The Google account-creation case separately checks metadata, locale and legal acceptance through the shared transfer-aware entry flow. |
| `signUpWithGoogleOneTap emits auth error event on failure` | The native `AuthEvent` bus is removed. `failedSignupPreservesStructuredError` checks a thrown error's status/code/long message/trace and no selected session. Presentation consumes the caller's failure and observed resources. |
| `signInWithOtp email creates sign in with strategy without preparing first factor` | Intentional request-shape change: the email case checks one identifier-only create followed by one selected email-code preparation. Both requests use the shared resource implementation. |
| `signInWithOtp phone creates sign in with strategy without preparing first factor` | The phone case checks the same explicit two-call sequence with `phone_code` and `phone_number_id`, not a native strategy shortcut. |
| `sessions exposes all sessions on the current client` | Every `SessionSelectionContractTest` scenario starts with two generated session resources and checks both IDs. The selected resource is distinct from the full session list. |
| `signOut with session ID removes only that session locally` | `selectedSessionSignoutLeavesRemainingSessionUnselected` checks the scoped POST, one remaining session and no selected user/session. The old native fallback that selected the next account and performed another client GET is intentionally removed. Shared `session-sign-out.test.mjs` additionally checks scoped/unscoped success, failure and cache behavior. |
| `setActive updates the active session locally` | `selectsExistingSessionWithoutFollowupRefresh` checks selection of the retained second-session resource after its server touch, with `select_session` and no additional client GET. The old always-`select_org` request and DTO fallback are removed. |
| `setActive sends select org intent with active organization id` | Existing packaged `SessionActivationTest` checks explicit organization selection and exact request fields. New `returnedOrganizationStateOverridesRequestedOrganization` checks `select_org` and the requested ID but retains the server's returned null organization. Unlike the old wrapper, Kotlin does not overwrite the response with the requested ID. |
| `setActive no-ops personal account selection when organization selection is forced` | `forcedPersonalSelectionKeepsCurrentSession` verifies explicit `Field.Null` organization selection is a no-op without HTTP. `omittedOrganizationStillSelectsSessionWhenOrganizationsAreForced` documents the distinct omission contract: selecting a session without changing its organization still proceeds. The old nullable default conflated these two meanings. |
| `setActive restores active session when response client sync clears local sessions` | Intentional removal of native resurrection: `emptyTouchClientDoesNotRestoreSession` verifies the empty server client clears sessions/user/selection even when the response also contains a session resource. Kotlin does not splice its previous DTO/user into the client. |
| `setActive keeps active session when follow-up client refresh omits it` | There is no extra Kotlin client refresh after activation. `laterEmptyRefreshClearsSelectedSession` verifies that a later explicit resource reload returning an empty client clears selection. The wrapper's `withActiveSessionFallback` behavior is removed. |
| `setActive keeps active session when follow-up refresh returns stale lastActiveSessionId` | `explicitSelectionOverridesStaleClientSelection` returns a touch envelope with both sessions but the previous last-active ID; shared `setActive` still selects the explicitly requested session. No follow-up GET or Kotlin fallback is required. Separate response-ordering tests check older client envelopes. |

## Verification

`bash scripts/run-auth-entry-contract.sh` runs the three new packaged suites separately and requires all 21 exact case names, with no missing, duplicate, failed or skipped cases. Running from outside the repository is supported. Each suite's report is retained before the next instrumentation run replaces the output directory. CI runs this gate on API 36 and preserves the preceding token reports.

The audit also uses eleven existing `PackagedCoreTest` cases, six `SessionActivationTest` cases and eight `EmailLinkCompletionTest` cases. Focused generated embedded checks cover 77 cases across Google/identifier, session activation/sign-out, magic links and browser SSO. Exact source hashes, executed case names and reports are retained in [the proof](evidence/auth-entry-points/proof.json).

Initial Google fixture failures were corrected before accepting evidence: trace identity belongs in the error document's `clerk_trace_id`, and a completed sign-up envelope must include the created client session for `SignUp.finalize`. These corrections changed tests, not the SDK. Existing generated Kotlin unused-expression warnings and Gradle deprecation warnings remain visible in the logs.

These deterministic checks do not close live authentication, signed-in old-major upgrade, physical-device prompt or performance gates. The [supported-profile exclusions](README.md#credential-continuity-and-unavailable-surfaces) continue to apply.
